import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { LoyalityCompetitionPage } from './loyality-competition';

@NgModule({
  declarations: [
    LoyalityCompetitionPage,
  ],
  imports: [
    IonicPageModule.forChild(LoyalityCompetitionPage),
  ],
})
export class LoyalityCompetitionPageModule {}
